{
  "$schema": "https://schema.up.railway.app/config.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "python manage.py limpiar_cupones_app",
    "restartPolicyType": "NEVER"
  }
}
