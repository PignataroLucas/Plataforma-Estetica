{
  "$schema": "https://schema.up.railway.app/config.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "python manage.py procesar_notificaciones",
    "restartPolicyType": "NEVER"
  }
}
